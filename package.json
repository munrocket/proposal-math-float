{
  "scripts": {
    "test": "node --experimental-modules --no-warnings test/test.mjs | node_modules/.bin/tap-prettify - ",
    "gen": "gcc test/gen.c -o gen.out -O2 -mfma && ./gen.out"
  },
  "devDependencies": {
    "tap-prettify": "0.0.2",
    "zora": "^4.0.1"
  }
}
